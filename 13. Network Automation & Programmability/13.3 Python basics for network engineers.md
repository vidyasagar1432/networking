---
tags: [automation, CCNA]
aliases: [Python basics for network engineers]
status: complete
difficulty: medium
exam: CCNA-200-301
---

## CCNA 200-301 Exam Prep

**CCNP ENCOR Relevance: Yes — Python scripting, APIs, automation frameworks, and infrastructure programmability are important CCNP Enterprise automation skills.**

---

# 1. Overview

Python is a high-level programming language widely used by network engineers to automate repetitive tasks, interact with APIs, collect device information, and manage large-scale networks. Cisco includes Python and automation concepts in modern networking because manual CLI configuration does not scale efficiently.

Python is commonly used with tools such as Netmiko, NAPALM, Ansible, REST APIs, and Cisco DNA Center.

---

# 2. Why Network Engineers Learn Python

Traditional network management:

```
Engineer

↓

SSH into device

↓

Type commands manually

↓

Repeat for 100 devices
```

---

Python automation:

```
Python Script

↓

Connect to devices

↓

Execute commands

↓

Collect results

↓

Generate reports
```

---

# 3. Python Uses in Networking

| Task | Python Usage |
|---|---|
| Device configuration | Push commands automatically |
| Monitoring | Collect device status |
| Backup | Save configurations |
| API interaction | Communicate with controllers |
| Troubleshooting | Analyze logs |
| Reporting | Generate reports |
| Security | Scan and audit devices |

---

# 4. Python Fundamentals

## Running Python

Interactive mode:

```bash
python
```

Example:

```python
print("Hello Network")
```

Output:

```
Hello Network
```

---

Script file:

```
script.py
```

Run:

```bash
python script.py
```

---

# 5. Python Syntax Basics

Python uses:

- Indentation
- New lines
- No semicolons required

Example:

```python
if True:
    print("Network Up")
```

---

# 6. Variables

A variable stores data.

Example:

```python
hostname = "R1"

ip_address = "192.168.1.1"
```

---

Network Example:

```python
device = "Router1"
interface = "GigabitEthernet0/0"
```

---

# 7. Python Data Types

| Type | Example | Usage |
|---|---|---|
| String | `"Cisco"` | Text |
| Integer | `10` | Numbers |
| Float | `99.5` | Decimal |
| Boolean | `True` | Status |
| List | `[]` | Multiple values |
| Dictionary | `{}` | Key-value data |

---

# 8. Strings

Example:

```python
vendor = "Cisco"
```

Access:

```python
print(vendor)
```

Output:

```
Cisco
```

---

String methods:

```python
hostname.upper()
```

Output:

```
ROUTER1
```

---

# 9. Lists

A list stores multiple values.

Example:

```python
interfaces = [
    "Gig0/0",
    "Gig0/1",
    "Gig0/2"
]
```

---

Access item:

```python
print(interfaces[0])
```

Output:

```
Gig0/0
```

---

Add item:

```python
interfaces.append("Gig0/3")
```

---

Network Example:

```python
devices = [
    "R1",
    "R2",
    "SW1"
]
```

---

# 10. Dictionaries

Stores key-value pairs.

Example:

```python
router = {
    "hostname":"R1",
    "ip":"192.168.1.1",
    "vendor":"Cisco"
}
```

---

Access value:

```python
print(router["hostname"])
```

Output:

```
R1
```

---

Common in API responses:

```json
{
 "hostname":"R1",
 "status":"up"
}
```

---

# 11. Tuples

Similar to lists but cannot be changed.

Example:

```python
location = (
    "India",
    "Hyderabad"
)
```

---

# 12. Sets

Stores unique values.

Example:

```python
vlans = {
10,
20,
30
}
```

---

# 13. Operators

## Arithmetic

| Operator | Meaning |
|---|---|
| + | Add |
| - | Subtract |
| * | Multiply |
| / | Divide |

---

Example:

```python
total = 10 + 5
```

---

# 14. Comparison Operators

| Operator | Meaning |
|---|---|
| == | Equal |
| != | Not equal |
| > | Greater |
| < | Less |

Example:

```python
status == "up"
```

---

# 15. Conditional Statements

## if statement

Example:

```python
status = "up"

if status == "up":
    print("Interface is working")
```

---

Output:

```
Interface is working
```

---

## if-else

```python
status = "down"

if status == "up":
    print("OK")
else:
    print("Check Interface")
```

---

# 16. Loops

Used for repeating tasks.

---

# For Loop

Example:

```python
devices = [
"R1",
"R2",
"R3"
]

for device in devices:
    print(device)
```

Output:

```
R1
R2
R3
```

---

Network Example:

```python
for router in routers:
    backup_config(router)
```

---

# While Loop

Runs while condition is true.

Example:

```python
count = 1

while count <= 5:
    print(count)
    count += 1
```

---

# 17. Functions

Functions group reusable code.

Example:

```python
def show_device():
    print("Router Information")

show_device()
```

---

Network Example:

```python
def backup_config(device):
    print("Backing up", device)
```

---

# 18. Function Parameters

Example:

```python
def ping_device(ip):
    print("Testing", ip)


ping_device("8.8.8.8")
```

Output:

```
Testing 8.8.8.8
```

---

# 19. Exception Handling

Used to handle errors.

Example:

```python
try:
    connection()
except:
    print("Connection failed")
```

---

Network Example:

```python
try:
    ssh.connect(device)
except:
    print("SSH failed")
```

---

# 20. File Handling

Network engineers often save:

- Config backups
- Logs
- Reports

---

Write file:

```python
file = open("backup.txt","w")

file.write("Router Config")

file.close()
```

---

Read file:

```python
file = open("backup.txt","r")

print(file.read())
```

---

# 21. Working with JSON

Python supports JSON for APIs.

Import:

```python
import json
```

---

Example:

```python
data = {
"device":"R1",
"status":"up"
}

json_data = json.dumps(data)

print(json_data)
```

Output:

```json
{
"device":"R1",
"status":"up"
}
```

---

# 22. Working with APIs

Install:

```bash
pip install requests
```

---

Example:

```python
import requests

response = requests.get(
"https://api.example.com/devices"
)

print(response.json())
```

---

# 23. Regular Expressions (Regex)

Used for searching text.

Common in:

- Logs
- CLI output
- IP extraction

---

Example:

```python
import re

text="Router IP 192.168.1.1"

result=re.findall(
r"\d+\.\d+\.\d+\.\d+",
text
)

print(result)
```

Output:

```
192.168.1.1
```

---

# 24. Python Libraries for Networking

| Library | Purpose |
|---|---|
| Netmiko | SSH automation |
| Paramiko | SSH connections |
| NAPALM | Multi-vendor automation |
| Requests | REST APIs |
| JSON | API data |
| PyYAML | YAML files |
| Ansible | Automation |

---

# 25. Netmiko Example

Install:

```bash
pip install netmiko
```

---

Example:

```python
from netmiko import ConnectHandler

device = {
"device_type":"cisco_ios",
"host":"192.168.1.1",
"username":"admin",
"password":"Cisco123"
}

connection = ConnectHandler(**device)

output = connection.send_command(
"show ip interface brief"
)

print(output)
```

---

# 26. Automation Workflow

Typical network automation:

```
Python Script

↓

Read Device List

↓

Connect using SSH/API

↓

Execute Commands

↓

Collect Output

↓

Generate Report
```

---

# 27. Python with CSV Files

Useful for device inventory.

Example:

CSV:

```
hostname,ip
R1,192.168.1.1
R2,192.168.1.2
```

Python:

```python
import csv

with open("devices.csv") as file:
    data = csv.reader(file)

    for row in data:
        print(row)
```

---

# 28. Virtual Environments

Used to isolate projects.

Create:

```bash
python -m venv network-env
```

Activate:

Linux:

```bash
source network-env/bin/activate
```

Windows:

```bash
network-env\Scripts\activate
```

---

# 29. Best Practices

| Practice | Reason |
|---|---|
| Use meaningful names | Readability |
| Comment code | Documentation |
| Handle errors | Reliability |
| Use version control | Tracking |
| Test in lab first | Avoid outages |
| Protect credentials | Security |

---

# 30. Network Automation Learning Path

Recommended order:

```
Python Basics

↓

Data Structures

↓

Files and JSON

↓

APIs

↓

SSH Automation

↓

Netmiko/NAPALM

↓

Ansible

↓

Cisco Automation
```

---

# CCNA Exam Traps

- Python is used for network automation.
- JSON is commonly used with REST APIs.
- YAML is commonly used with Ansible.
- Dictionaries store key-value pairs.
- Lists store multiple values.
- Loops automate repeated tasks.
- Functions improve code reuse.
- Exception handling prevents script failures.
- Netmiko is used for SSH automation.
- Requests library is used for REST APIs.

---

# Quick Review Table

| Topic | Key Point |
|---|---|
| Variable | Stores data |
| List | Multiple values |
| Dictionary | Key-value data |
| Loop | Repeat tasks |
| Function | Reusable code |
| JSON | API data format |
| Requests | REST API communication |
| Netmiko | SSH automation |
| NAPALM | Multi-vendor automation |
| Ansible | Automation framework |

---

# Practice Questions

## Question 1

Which Python data structure stores key-value pairs?

A. List  
B. Dictionary  
C. Tuple  
D. Set  

### Answer: B

---

## Question 2

Which library is commonly used for Cisco SSH automation?

A. Requests  
B. Netmiko  
C. Flask  
D. NumPy  

### Answer: B

---

## Question 3

Which Python library is used for REST APIs?

A. Requests  
B. Netmiko  
C. Tkinter  
D. Django  

### Answer: A

---

## Question 4

Which format is commonly returned by REST APIs?

A. JSON  
B. EXE  
C. Binary  
D. HTML only  

### Answer: A

---

## Question 5

What is the purpose of loops in Python?

A. Store passwords  
B. Repeat tasks automatically  
C. Create hardware  
D. Replace IOS  

### Answer: B